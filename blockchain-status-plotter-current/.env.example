# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: Override default model (gpt-4o-mini is cost-effective)
OPENAI_MODEL=gpt-4o-mini

# The Graph Configuration
# Your deployed subgraph URL (if available)
NEXT_PUBLIC_SUBGRAPH_URL=https://api.studio.thegraph.com/query/<your-subgraph-id>/pox/version/latest

# Filecoin Configuration (optional - has defaults)
FILECOIN_CLOUD_RPC=https://api.calibration.node.glif.io/rpc/v1

# Rate Limiting (optional - has defaults)
# API_RATE_LIMIT=60
# API_RATE_WINDOW_MS=60000
