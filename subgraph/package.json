{
  "name": "pox-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio pox",
    "create-local": "graph create --node http://localhost:8020/ pox",
    "remove-local": "graph remove --node http://localhost:8020/ pox",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pox"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2"
  }
}
